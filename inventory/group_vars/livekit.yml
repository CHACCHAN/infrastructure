# LiveKit VMの役割プロファイル。
---
# --- ① VMプロファイル(pveドメインが使う) ---
pve_vm_cores: 8
pve_vm_memory: 4096
pve_vm_disk_size: 32

# --- ② SSH接続(cloud-initが作るユーザーと鍵) ---
pve_ssh_user: livekit
pve_ssh_pubkey_file: ~/.ssh/id_ed25519_livekit.pub
pve_ssh_prikey_file: ~/.ssh/id_ed25519_livekit
