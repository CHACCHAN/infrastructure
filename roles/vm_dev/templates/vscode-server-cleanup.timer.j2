# Ansible(roles/vm_dev)が生成。手動編集は次回実行で上書きされる。
[Unit]
Description=Periodically clean up old VSCode Server (Remote-SSH) versions

[Timer]
OnCalendar={{ dev_vscode_cleanup_schedule }}
Persistent=true

[Install]
WantedBy=timers.target
