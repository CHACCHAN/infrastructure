# Ansible(roles/vm_dev)が生成。手動編集は次回実行で上書きされる。
[Unit]
Description=Clean up old VSCode Server (Remote-SSH) versions

[Service]
Type=oneshot
User={{ ansible_user }}
ExecStart=/usr/local/bin/vscode-server-cleanup.sh
