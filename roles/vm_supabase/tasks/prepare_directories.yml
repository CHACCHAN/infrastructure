# 設置ディレクトリだけを用意する。データの置き場所(volumes/db/data など)は
# compose側が相対パスで作るため、ここでは作らない
# (所有者やモードを先に決めてしまうと、コンテナ側の初期化とぶつかる)。
---
- name: Supabaseの設置ディレクトリを作成する
  ansible.builtin.file:
    path: "{{ supabase_install_dir }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  become: true
