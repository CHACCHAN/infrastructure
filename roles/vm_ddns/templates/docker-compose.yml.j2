# Ansible(roles/vm_ddns)が生成。手動編集は次回実行で上書きされます。
#
# 内容はDocker Hubの公式例と同じ構成。差分は次の2点:
#   - VMからの公開ポートと待ち受けアドレスを変えられるようにしている
#   - ログの時刻をVMのタイムゾーンに合わせるため TZ を渡している
#
# アプリの設定(APIトークン・ゾーン・レコード)は config/config.json 側です。
# コンテナ内の待ち受けポートは8080で固定です(アプリが直接指定しているため)。
---
services:
  cloudflare-ddns-ui:
    container_name: {{ ddns_container_name }}
    image: {{ ddns_image }}:{{ ddns_version }}
    restart: unless-stopped
    environment:
      TZ: {{ vm_timezone }}
{% for key, value in (ddns_extra_env | dictsort) %}
      {{ key }}: {{ value | to_json }}
{% endfor %}
    ports:
{% if ddns_http_bind in ['0.0.0.0', '::', ''] %}
      - "{{ ddns_http_port }}:8080/tcp"
{% else %}
      # ダッシュボードには認証が無いため、待ち受けアドレスを絞っている
      - "{{ ddns_http_bind }}:{{ ddns_http_port }}:8080/tcp"
{% endif %}
    volumes:
      # 設定(APIトークンを含む)
      - ./config:/app/config
      # 動作ログ(ddns.log)と更新履歴(record_stats.json)
      - ./logs:/app/logs
