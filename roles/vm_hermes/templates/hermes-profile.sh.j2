# Ansible(roles/vm_hermes)が生成。手動編集は次回実行で上書きされます。
# SSHでログインしてhermes CLIを使うときの環境。
export HERMES_HOME={{ hermes_home }}
# インストーラが置くランチャ(~/.local/bin/hermes)をPATHへ入れる
case ":$PATH:" in
  *":$HOME/.local/bin:"*) ;;
  *) export PATH="$HOME/.local/bin:$PATH" ;;
esac
