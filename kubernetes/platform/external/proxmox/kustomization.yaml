# Proxmox VE (ハイパーバイザ WebUI) — 172.16.11.11-20:8006
#
# 10 ノードそれぞれを独立した Service / EndpointSlice / Ingress として公開する。
# ServersTransport だけは 10 ノードで共有する。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serverstransport.yaml
  - pbs.yaml
  - pve01.yaml
  - pve02.yaml
  - pve03.yaml
  - pve04.yaml
  - pve05.yaml
  - pve06.yaml
  - pve07.yaml
  - pve08.yaml
  - pve09.yaml
  - pve10.yaml
