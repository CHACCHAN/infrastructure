# Proxmox VE ノード 10 — 172.16.11.20:8006 (自己署名 HTTPS)
#
# ノードを増やすときはこのファイルをコピーして、
# 名前・IP・ホスト名の 3 か所を書き換え、kustomization.yaml に 1 行足す。
apiVersion: v1
kind: Service
metadata:
  name: pbs-external
  namespace: platform
  labels:
    app.kubernetes.io/name: pbs
    app.kubernetes.io/component: external-service
    app.kubernetes.io/managed-by: kustomize
  annotations:
    # WebUI は HTTPS (8006) で待ち受けている。
    traefik.ingress.kubernetes.io/service.serversscheme: "https"
    # 証明書が自己署名なので、検証をスキップする ServersTransport を使う。
    traefik.ingress.kubernetes.io/service.serverstransport: "platform-proxmox-transport@kubernetescrd"
spec:
  # selector を書かない。エンドポイントは下の EndpointSlice で手動管理する。
  type: ClusterIP
  ports:
    - name: https
      protocol: TCP
      port: 8007
      targetPort: 8007
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: pbs-external
  namespace: platform
  labels:
    app.kubernetes.io/name: pbs
    app.kubernetes.io/component: external-service
    app.kubernetes.io/managed-by: kustomize
    # このラベルで上の Service と紐付く。値は Service 名と完全に一致させること。
    kubernetes.io/service-name: pbs-external
addressType: IPv4
ports:
  - name: https
    protocol: TCP
    port: 8007
endpoints:
  - addresses:
      - "172.16.11.7"
    # クラスタ外ホストの死活は kube-proxy が判定できないため、常に Ready 扱いにする。
    conditions:
      ready: true
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pbs
  namespace: platform
  labels:
    app.kubernetes.io/name: pbs
    app.kubernetes.io/component: external-service
    app.kubernetes.io/managed-by: kustomize
  annotations:
    # HTTPS 専用で公開する。平文 HTTP (:80) では 404 になる。
    traefik.ingress.kubernetes.io/router.entrypoints: "websecure"
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - "pbs.cc-chacchan.com"
      # platform/certificates/ の Certificate が作るワイルドカード証明書
      secretName: cc-chacchan-wildcard-tls
  rules:
    - host: "pbs.cc-chacchan.com"
      http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
              service:
                name: pbs-external
                port:
                  name: https
