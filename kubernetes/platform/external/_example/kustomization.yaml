# クラスタ外サービスのひな形。使い方は README.md を参照。
#
# このディレクトリは platform/external/kustomization.yaml から
# 参照されていないので、クラスタには適用されない。
# コピーして使うこと:  cp -r platform/external/_example platform/external/myapp
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service.yaml
  - ingress.yaml
