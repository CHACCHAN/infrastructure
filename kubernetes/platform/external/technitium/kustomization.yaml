# Technitium DNS (DNS サーバ) — 172.16.11.3
#
# 1 つのバックエンドに対して WebUI (5380) と DoH (8053) の 2 経路を張る。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - service.yaml
  - middleware.yaml
  - ingress.yaml
